#include<stdio.h>
/**
 * main - ENTRY POINT | A program that print a line using the printf function
 * Return: ALWAYS | 0 (Success)
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
